# This is a Databricks asset bundle definition for spotify_dad.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dad
  uuid: 9273fe91-2617-4b87-8c95-072ee6e01bb3

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: http://adb-7405606594199672.12.azuredatabricks.net
      root_path: /Workspace/PROD/eswararumugam03_gmail.com#ext#@eswararumugam03gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: spotify
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: http://adb-7405606594199672.12.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/eswararumugam03_gmail.com#ext#@eswararumugam03gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/eswararumugam03_gmail.com#ext#@eswararumugam03gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: spotify
      schema: prod
    permissions:
      - user_name: eswararumugam03_gmail.com#ext#@eswararumugam03gmail.onmicrosoft.com
        level: CAN_MANAGE
 this the fill databricks.yml file what we want to change give the the me